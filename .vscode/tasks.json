{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Save",
            "type": "shell",
            "command": "${command:python.interpreterPath}",
            "args": [
                "${workspaceFolder}/save.py"
            ],
            "group": "build"
        }
    ]
}